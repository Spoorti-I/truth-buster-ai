<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main Dark Theme -->
    <style name="Theme.TruthBusterAI" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_red</item>
        <item name="colorPrimaryVariant">@color/status_bar</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_indigo</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash Theme (fullscreen, no action bar) -->
    <style name="Theme.TruthBusterAI.Splash" parent="Theme.TruthBusterAI">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
