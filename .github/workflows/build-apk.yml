name: 📱 Build Truth Buster AI Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 🔨 Make Gradle Executable
        run: chmod +x TruthBusterApp/gradlew || true

      - name: 🏗️ Build Debug APK
        run: |
          cd TruthBusterApp
          gradle assembleDebug --no-daemon

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TruthBusterAI-v2.0-APK
          path: TruthBusterApp/app/build/outputs/apk/debug/app-debug.apk
